SNIPER AI - DOWNLOAD
====================

EA:
https://github.com/theteletsak-max/NEUROX-SCALERT-AI/blob/cursor/sniper-ai-compile-fix-b12d/SNIPER_AI_OK36.mq5

BUILD_ID: SA_PRISM_REV_CORRECT_36

Correct reversal signals:
- BUY needs sell-side sweep (lows) + zone + reclaim
- SELL needs buy-side sweep (highs) + zone + reclaim
- Validates and logs PASS/FAIL reason

INSTALL: delete old SNIPER* → F7 → re-attach → EntryTF=H4 → BUILD_ID=SA_PRISM_REV_CORRECT_36
